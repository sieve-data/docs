---
title: "Object Tracking"
description: "Building a video localization app"
---

Let's build an app that can object tracking Sieve app.

Start by cloning the sieve examples git repository and cd into the `yolo_object_tracking` directory.

```sh
git clone https://github.com/sieve-community/examples
cd yolo_object_tracking
```

(TODO: More)
