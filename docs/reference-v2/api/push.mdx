---
title: "Push Data"
api: "POST /v2/push"
---

<ParamField body="function" type="string">
  name of Sieve function. Possible values are: `<organization-name>/<function-name>` or `<organization-name>/<function-name>:<version-id-prefix>` where the version-id prefix is 4 or more characters of your function version.

  An example would be: `sieve/video_retalking` or `sieve/video_retalking:1a8b41cc`.
</ParamField>

<ParamField body="id" type="string">
  ID of Sieve model
</ParamField>

<ParamField body="inputs" type="dict" required>
  an array representing the inputs to the sieve function. Read more about inputs [here](/guide/sieve/inputs-and-outputs)
</ParamField>


<ParamField body="webhook_urls" type="array">
  an array of URLs to be called when the job completes. Read more about webhooks [here](/guide/custom-apps/webhooks)
</ParamField>

This endpoint triggers either a function or a model with a given set of inputs. The structure of the inputs should match the inputs types specified in the function or model definition.

## Example
A function that takes in a `sieve.Video` named `video` and a `sieve.Image` named `image` would be triggered with the following request:
```json
{
  "function": "sieve/sample_function",
  "inputs": {
    "video": {
      "url": "{SOME_VIDEO_URL}"
    },
    "image": {
      "url": "{SOME_IMAGE_URL}"
    }
  }
}
```

A function that takes in a `string` named `prompt` would be triggered with the following request:
```json
{
  "function": "sieve/some_function",
  "inputs": {
    "prompt": "some string"
  }
}
```

### Webhook Response

The webhook responses vary depending on the event.

### Event `job_complete`

<ResponseField name="type" type="string">
The type of event. Always `job_complete`.
</ResponseField>
<ResponseField name="body" type="object">
The event data.
<ResponseField name="status" type="string">
The status of the job.
</ResponseField>
<ResponseField name="job_id" type="string">
The job ID.
</ResponseField>
<ResponseField name="output" type="object">
Output field will be null if the job errored out.
<Expandable title="properties">
<Snippet file="output.mdx" />
</Expandable>
</ResponseField>
<ResponseField name="error" type="string">
Will be null if no error occurred.
</ResponseField>
</ResponseField>

<!-- It puts a weird empty example unless I do this here -->
<ResponseExample>
</ResponseExample>

```json Event: Job Completed
{
  "body": {
    "job_id": "ddb9572f-b829-4ecb-bd3f-91ebb0f0c777",
    "status": "error",
    "output": null,
    "error": "..."
  },
  "type": "job_complete"
}
```

### Event `job_start`

<ResponseField name="type" type="string">
The type of event. Always `job_start`.
</ResponseField>
<ResponseField name="body" type="object">
The event data.
<ResponseField name="job_id" type="string">
The job ID.
</ResponseField>
</ResponseField>

```json Event: Job Started
{
  "body": {
    "job_id": "ddb9572f-b829-4ecb-bd3f-91ebb0f0c777"
  },
  "type": "job_complete"
}
```
