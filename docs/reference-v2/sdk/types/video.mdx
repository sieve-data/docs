---
title: "sieve.Video"
---

```python
class Video(sieve.File)
```

A type that represents a video in Sieve. The benefit of using a `sieve.Video` is that Sieve is able to optimize the storage and transfer of the type while processing, which results in faster processing times.

### cap
```python
@property
cap(self) -> cv2.VideoCapture:
```

The `cv2.VideoCapture` object that can used to read the video.

### fps
```python
@property
fps(self) -> float:
```

The frame rate of the video.

### frame_count
```python
@property
frame_count(self) -> int:
```

The number of frames in the video.

### width
```python
@property
width(self) -> int:
```

The width of the video.

### height
```python
@property
height(self) -> int:
```

The height of the video.

### channels
```python
@property
channels(self) -> int:
```

The number of channels in the video.

### __getstate__
```python
def __getstate__(self) -> dict:
```

### dict
```python
def dict(self) -> dict:
```

### json
```python
def json(self) -> str:
```
