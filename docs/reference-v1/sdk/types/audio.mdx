---
title: "sieve.Audio"
---

```python
class Audio(sieve.File)
```

A type that represents an audio file in Sieve. The benefit of using a `sieve.Audio` is that Sieve is able to optimize the storage and transfer of the type while processing, which results in faster processing times.

